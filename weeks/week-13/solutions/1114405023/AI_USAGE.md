
---

# `AI_USAGE.md`

```markdown
# AI 使用說明

## 使用工具

本次作業有使用 ChatGPT 協助整理程式架構、測試項目與 Markdown 文件內容。

## 使用方式

AI 主要協助以下內容：

1. 根據作業要求規劃檔案結構。
2. 協助撰寫 `load_year()`、`get_top_depts()`、`load_county_counts()`、`get_top_counties()` 等函式。
3. 協助撰寫 matplotlib 圖表程式。
4. 協助撰寫 unittest 測試案例。
5. 協助整理 README、REPORT、TEST_LOG 文件格式。

## 自行確認與修改

我有自行確認以下內容：

1. 程式可以從正確的 `assets/stu-data` 路徑讀取 CSV。
2. CSV 使用 `utf-8-sig` 編碼讀取。
3. `output/task1.png` 與 `output/task2.png` 可以正常產生。
4. unittest 測試可以正常通過。
5. REPORT.md 的觀察內容依照實際圖表結果修正。
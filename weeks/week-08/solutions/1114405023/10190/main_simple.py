# 10190 simple version scaffold
# Waiting for official sample / complete formula verification.

n, w, t, v = map(int, input().split())
umbrellas = [tuple(map(int, input().split())) for _ in range(n)]

print("10190 needs official sample or full statement to complete exactly.")
